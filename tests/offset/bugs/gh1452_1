puts "============"
puts "Crash on negative offset applied to a small circular wire"
puts "============"
puts ""

pload MODELING

set diameter 2.01
set padLength 3.0
set taperDegrees -1.00
set offset [dval ($taperDegrees*pi/180.0)*$padLength]

circle c 0 0 0 $diameter/2.0
mkedge e c
wire   w e
compound w cc
mkoffset res cc 1 $offset i
checkprops res_1 -l 5.98561
top
fit
xwd ${::imagedir}/${::casename}.png

# for 'end' script
renamevar res_1 result
