puts "================================"
puts "GH1418: AdvApp2Var Jacobi maxima is written to the wrong workspace slot"
puts "================================"

pload MODELING

sphere s 10
approxsurf r s 1.e-3 0 0 8 8 100
set d [dumpjson r]

regexp {"udeg": ([0-9]*)} $d full udeg
regexp {"vdeg": ([0-9]*)} $d full vdeg
if { $udeg != 7 } { puts "Error: wrong U degree" }
if { $vdeg != 7 } { puts "Error: wrong V degree" }

donly r
axo
fit
xwd $imagedir/${casename}.png
